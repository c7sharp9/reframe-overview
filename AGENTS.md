# AGENTS.md

Read `CLAUDE.md` in this folder first. It is the orientation file for any AI agent working here: what the project is, the stack, how to run it, how it deploys, and the gotchas. Then read `ROADMAP.md` for dated next steps, and `memory/` for the session stories behind past work.

The global operating brief for Codex (how Jonathan's projects are organized, which doc holds which kind of note, commit prefix, handoff note) is at `~/.codex/AGENTS.md`, a symlink to the catalog's `how-to-work-with-codex.md`.
